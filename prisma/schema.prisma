generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
}

model Payment {
  id         Int     @id @default(autoincrement())
  userNumber String?
  userYear   String?
  userCity   String?
  userName   String?
  userLink   String?
}
