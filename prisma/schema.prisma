generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Payment {
  id         Int     @id @default(autoincrement())
  userNumber String?
  userYear   String?
  userCity   String?
  userName   String?
  userLink   String?
}
